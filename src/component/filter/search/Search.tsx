import Input from "../../../ui/input/Input";

const Search = () => {
  return (
    <div>
      <Input value={""} placeholder={"Поиск"} setValue={() => {}} />
    </div>
  );
};

export default Search;
